.row
	ul.breadcrumbs
	  li
	    = link_to 'Admin', admin_path
	  li.current
	    = link_to 'Merit Categories', admin_merit_categories_path
	.small-12.small-centered.columns
		h2 
			| Merits Categories 
			= link_to "New Merit Category", new_admin_merit_path, class: "button new_merit_button mini-button"
		- if @merits
			table#merit_index_table
				thead
					tr 
						th Name
						th In Use?
						th
						th
				tbody
					- @merits_categories.each do |merit_category|
						tr
							td
								= link_to merit_category.name, admin_merit_category_path(id: merit.id)
							td
								= (merit_category.status) ? "Yes" : "No"
							td
								= link_to "Edit", edit_admin_merit_category_path(merit_category)
							td
								= form_tag(admin_merit_category_path(merit_category), method: 'DELETE') do
									= button_tag "Delete", type: 'submit', onClick: 'return confirm("Are you sure you want to delete this merit category?");', class: 'button mini-button'
							
		- else
			| There are no merits entered yet. <a href="#{new_admin_merit_path}">Create one now!</a>
